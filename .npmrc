# npm prints a "> pkg@version script" banner to stdout before running a script.
# The analyzer must emit nothing on stdout except the JSON report, so npm's own
# logging is silenced here; errors from the analyzer still go to stderr.
loglevel=silent
